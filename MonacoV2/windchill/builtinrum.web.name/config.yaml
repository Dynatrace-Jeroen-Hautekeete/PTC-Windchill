configs:
- id: PTC
  config:
    template: windchill.json
    skip: false
  type:
    settings:
      schema: builtin:rum.web.name
      schemaVersion: 1.0.2
      scope:
        configId: PTC
        configType: application-web
        property: id
        type: reference